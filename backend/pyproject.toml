[project]
name = "url-shorter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hevizy", email = "hevizy134@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.20.0",
    "fastapi>=0.141.1",
    "hashids>=1.3.1",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
url-shorter = "url_shorter:main"

[tools.uv]
package = false
